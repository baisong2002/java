1.语句
    语录以分号结尾，分号前没有语句，则javascript认为是空语句，不写分号，浏览器会自动添加，有时候会加错分号
2.变量
    语录以分号结尾，分号前没有语句，则javascript认为是空语句，进行格式化
3、输出语句：
  arlert();弹窗
  document.write();在页面显示
  console.log();在控制台输出
js编写位置：
    可以编写在外部的js文件中，通过script标签引入
    script标签里
    onclick属性中
    herf属性中
script标签一旦用于引入外部标签，就不能在编写了，可以使用内部js代码
从上到下一行一行执行
4、js注释：
多行：/*     */
单行：//
js严格区分大小写
一、字面量和变量
   字面量：不可改变的量，可以直接使用，但是我们一般都不会直接使用字面量
   变量：变量可以用来保存字面量，变量的值可以任意改变
 1、声明变量：
    var 变量名;
二、标识符
  在JS中可以自主命名的都可以成为标识符
  规则：
    标识符可以含有字母、数字 、_、$
    标识符不能以数字开头
    标识符不能是ES中的关键字或保留字
    标识符一般都采用驼峰命名法
                      首字母小写，每个单词的开头大写，其余字母小写
三、数据类型：
字面量类型，6种数据类型
\作为转义字符
 1、字符串（String）:需要使用""引起来，双引、单引都可以，不要混着用，引号不能嵌套（双引不能双引，单引不能单引）
 2、数值（Number）：整数和浮点数也是数值，可以运算符typeof检查变量的类型
   JS表示数值的最大值Number.MAX_VALUE，超过最大值返回Infinity
   NaN特殊值，类型是数值，表示not A Number 
 3、布尔值（Boolean）：true/flse 真/假  用来逻辑判断
 4、空值（Null）：null  用来表示一个为空的对象
 5、未定义（Undefined）
 基本数据类型
 6、对象（Object）
 7、数组：
   var a=[数据1、数据2、...]
四、函数:
 1、声明函数
 function 函数名(){
     函数体；
 }
 立即执行函数：
   定义完立即执行，只会执行一次
函数返回值：return
变量复制声明函数叫做表达式，创建的函数，函数名只在当前函数区块内有效
构造函数：
  var 函数名=function(参数1,参数2,函数体);
  var a=function(1,2,return 1+2);
调用函数：
   要使用圆括号，括号内加入函数的参数
五、对象
生成方法:
 var 对象名={
     键名(属性)：键值,(键值对)
 }
键名是数值则会直接转换为字符串，如果键名不符合规则，则必须加引号
如果键值是函数则被称作为方法，可以像函数一样调用
如果属性是对象，则形成链式引用
读取属性：
  对象名.键值名
  对象名['键值名']
  如果不用''引起来则会被认为是变量
允许"后绑定"    对象名.键值名="";
查看属性 Object.keys(对象名);
删除属性   delete 对象名.属性  无法删除继承的属性
查看对象是否包含某个属性  '属性' in 对象名    无法识别属性是自身的还是继承的   判断属性是自身的还是继承的 hasOwnProperty
遍历属性:
  for(var i in 对象名){
      console,log(i);
  }
会跳过不可遍历的属性，可以便利继承的属性，不会遍历到toString属性
多个对象操作：
 with(对象){
     语句;
 }
如果with区块有变量复制操作，则该变量必须是当前对象已经存在的属性，否则会创建一个全局变量